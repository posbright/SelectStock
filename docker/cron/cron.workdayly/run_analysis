#!/bin/bash
# 数据分析任务 — 本地分析计算（指标/K线/策略/回测），Docker 版
# 与 run_fetch 配合使用，实现获取与分析解耦
# 内置"跳过已完成"检查，INSTOCK_FORCE_ANALYSIS=1 可强制执行
PROJECT_ROOT=/data/InStock
source /etc/cron/_common.sh

init_env
LOG_FILE=$LOG_DIR/workdayly.log

check_trade_day "数据分析"
run_job "数据分析 (analysis_daily_job)" "instock/job/analysis_daily_job.py"
