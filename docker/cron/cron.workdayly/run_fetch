#!/bin/bash
# 数据获取任务 — 集中执行所有外部 API 数据采集（Docker 版）
PROJECT_ROOT=/data/InStock
source /etc/cron/_common.sh

init_env
LOG_FILE=$LOG_DIR/workdayly.log

check_trade_day "数据获取"
run_job "数据获取 (fetch_daily_job)" "instock/job/fetch_daily_job.py"
