# ============================================================
# InStock Docker 环境配置文件
# ============================================================
# 复制此文件为 .env 并根据需要修改
# 完整变量列表见项目根目录 .env.example
# ============================================================

# ============ 数据库配置 ============
DB_PASSWORD=root
DB_DATABASE=instockdb

# ============ 端口配置 ============
# Web服务端口
WEB_PORT=9988
# Supervisor管理端口
SUPERVISOR_PORT=9001

# ============ 数据源配置 ============
# DATA_SOURCE_MAX_RETRIES=2          # 数据源最大重试次数
# DATA_SOURCE_RETRY_INTERVAL=30      # 数据源重试间隔（秒）
# HIST_DATA_DEFAULT_YEARS=15         # 默认获取历史数据年数

# ============ 并发控制 ============
# INSTOCK_BATCH_SIZE=50              # 流式分析每批股票数
# INSTOCK_ANALYSIS_WORKERS=2         # 流式分析并发线程数
# INSTOCK_INDICATOR_WORKERS=4        # 指标计算并发线程数
# INSTOCK_KLINE_PATTERN_WORKERS=4    # K线形态识别并发线程数
# INSTOCK_STRATEGY_WORKERS=4         # 策略计算并发线程数（内层）
# INSTOCK_STRATEGY_OUTER_WORKERS=2   # 策略外层并发数（按策略表）
# INSTOCK_KLINE_CACHE_WORKERS=2      # K线缓存更新并发数
# INSTOCK_BACKTEST_OUTER_WORKERS=1   # 回测外层并发
# INSTOCK_BACKTEST_INNER_WORKERS=2   # 回测内层并发
# INSTOCK_BATCH_DATE_WORKERS=3       # 批量日期作业并发数
# INSTOCK_CRAWL_WORKERS=5            # 数据爬取并发线程数
# INSTOCK_DB_CONN_RETRIES=3          # 数据库操作重试次数

# ============ 子进程超时 ============
# INSTOCK_JOB_TIMEOUT=1800           # 子进程默认超时（秒）
# INSTOCK_KLINE_JOB_TIMEOUT=36000    # K线缓存子进程超时（秒）

# ============ 数据结算 ============
# INSTOCK_SETTLEMENT_HOUR=18         # API 数据结算时间（小时，收盘后数据延迟发布截止）

# ============ 远程数据库配置 ============
# 使用 docker-compose.remote-db.yml 时必须设置以下变量
# REMOTE_DB_HOST 和 REMOTE_DB_PASSWORD 为必填项
REMOTE_DB_HOST=your-db-host
REMOTE_DB_PORT=3306
REMOTE_DB_USER=root
REMOTE_DB_PASSWORD=your-password
REMOTE_DB_DATABASE=instockdb
